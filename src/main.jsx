import React from 'react'
import ReactDOM from 'react-dom/client'
import FlashCards from './FlashCards.jsx' // ดึงโค้ด Flashcard มาแสดงผล

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FlashCards />
  </React.StrictMode>,
)
